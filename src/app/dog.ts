export class Dog {
    id: string;
    name: string;
    thumbnail: string;

    constructor (id, name, thumbnail) {
        this.id = id;
        this.name = name;
        this.thumbnail = thumbnail;
    }
}
